Config tracking for my local home server. Hostname `kakariko`.

### Server setup

I have a bare repo on the server and a non-bare repo that is cloned from it.
I push my changes to GitHub and the server's git repo via SSH and then clone on the server from the server's bare repo.
This could just pull from GitHub as well in this case since the repo is public.
